server:
  port: 8888
spring:
  application:
    name: aiDemo2
  ai:
    # ❗ 移除 alibaba 层级，直接在 ai 下配置 dashscope
    dashscope:
      api-key: ${DASHSCOPE_API_KEY} # 使用环境变量注入
    embedding:
      # ???? DashScope ??? embedding ??
      model: text-embedding-v1
